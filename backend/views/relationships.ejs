<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Relationships - <%= paper.title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/common.css">
  <link rel="stylesheet" href="/css/relationships.css">
</head>
<body>
  <div class="container">
    <div class="header-card">
      <div class="back-link-section">
        <a href="/paper/<%= paperId %>" class="back-link">
          <i class="bi bi-arrow-left"></i> Back to Paper
        </a>
      </div>
      <div class="header-gradient">
        <h1 class="mb-2" style="font-size: 2rem; font-weight: 700;">
          <i class="bi bi-arrow-left-right"></i> Relationships
        </h1>
        <p class="mb-0 opacity-90"><%= paper.title %></p>
      </div>
      <div class="card-body p-4">
        <div class="d-flex align-items-center gap-2">
          <span class="badge bg-primary fs-6 px-3 py-2">
            <i class="bi bi-link-45deg"></i> <%= relationships.length %> relationship(s)
          </span>
        </div>
      </div>
    </div>
    
    <div class="table-card">
      <% if (relationships.length > 0) { %>
        <div class="table-responsive">
          <table class="table table-hover mb-0">
            <thead>
              <tr>
                <th><i class="bi bi-arrow-right-circle"></i> Source</th>
                <th><i class="bi bi-arrow-left-right"></i> Relation Type</th>
                <th><i class="bi bi-arrow-left-circle"></i> Target</th>
              </tr>
            </thead>
            <tbody>
              <% relationships.forEach(rel => { %>
                <tr>
                  <td>
                    <span class="source-target"><%= rel.sourceLabel %></span>
                    <span class="id-badge"><%= rel.source %></span>
                  </td>
                  <td class="relation-type">
                    <%= rel.relationType.replace(/_/g, ' ') %>
                  </td>
                  <td>
                    <span class="source-target"><%= rel.targetLabel %></span>
                    <span class="id-badge"><%= rel.target %></span>
                  </td>
                </tr>
              <% }); %>
            </tbody>
          </table>
        </div>
      <% } else { %>
        <div class="text-center text-muted py-5">
          <i class="bi bi-inbox" style="font-size: 4rem; opacity: 0.3;"></i>
          <p class="mt-3 fs-5">No relationships found for this paper.</p>
        </div>
      <% } %>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
